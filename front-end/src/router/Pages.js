export const LoginPage = (history) => {
  history.push('/login');  
}

export const SignupPage = (history) => {
  history.push('/signup');
}

export const InsertImagePage = (history) => {
  history.push('/insert')
}

export const ViewImagePage = (history) => {
  history.push('/view')
}